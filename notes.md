reverting to last good git codebase

git status                      # sanity check
git stash -u                    # optional backup (keeps a safety copy)
git reset --hard HEAD           # nuke all local changes


